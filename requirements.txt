# AI Friend - 二次元桌面助手
# Python 3.11+

# GUI 框架
PySide6>=6.6.0

# Live2D 渲染 (WebEngine 方案，不再需要 PyOpenGL/live2d-py)
# pixi-live2d-display 等 JS 库已本地化到 assets/live2d_web/libs/

# LangChain 核心
langchain>=1.2.0
langchain-core>=1.2.0
langchain-community>=0.4.0
langchain-text-splitters>=1.1.0

# LangChain 模型支持
langchain-openai>=1.1.0
langchain-ollama>=1.0.0

# 定时任务
apscheduler>=3.10.4

# 数据验证
pydantic>=2.5.0
pydantic-settings>=2.0.0

# HTTP 请求
requests>=2.31.0

# 系统信息
psutil>=5.9.6

# 剪贴板
pyperclip>=1.8.2

# 环境变量
python-dotenv>=1.0.0

# MCP (Model Context Protocol)
langchain-mcp-adapters>=0.2.2

# macOS 系统集成（隐藏 Dock 图标）
pyobjc-framework-Cocoa>=10.0; sys_platform == 'darwin'
